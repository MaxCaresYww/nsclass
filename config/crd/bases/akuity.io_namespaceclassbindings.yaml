---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespaceclassbindings.akuity.io
spec:
  group: akuity.io
  names:
    kind: NamespaceClassBinding
    listKind: NamespaceClassBindingList
    plural: namespaceclassbindings
    shortNames:
    - nsclsbind
    - nsclsb
    singular: namespaceclassbinding
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .spec.mappings[*].namespace
      name: Namespaces
      type: string
    - jsonPath: .metadata.generation
      name: Generation
      priority: 1
      type: integer
    - jsonPath: .status.observedGeneration
      name: Observed Generation
      priority: 1
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NamespaceClassBinding is the Schema for the namespaceclassbindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NamespaceClassBinding
            properties:
              mappings:
                description: mappings assigns NamespaceClasses to target namespaces.
                items:
                  description: NamespaceClassBindingMapping assigns NamespaceClasses
                    to one namespace.
                  properties:
                    classNames:
                      description: classNames is the list of NamespaceClasses to apply
                        to the target namespace.
                      items:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                    namespace:
                      description: namespace is the target namespace to reconcile
                        resources into.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - classNames
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - namespace
                x-kubernetes-list-type: map
            required:
            - mappings
            type: object
          status:
            description: status defines the observed state of NamespaceClassBinding
            properties:
              conditions:
                description: conditions represent the current state of the NamespaceClassBinding
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              namespaces:
                description: namespaces records the resources last applied by namespace.
                items:
                  description: NamespaceClassBindingNamespaceStatus records applied
                    resources for one namespace.
                  properties:
                    namespace:
                      description: namespace is the namespace where resources were
                        applied.
                      type: string
                    resources:
                      description: resources records the resources last applied in
                        this namespace.
                      items:
                        description: NamespaceClassBindingAppliedResource identifies
                          one resource applied by a binding.
                        properties:
                          apiVersion:
                            description: apiVersion is the applied resource API version.
                            type: string
                          className:
                            description: className is the NamespaceClass that supplied
                              this resource.
                            type: string
                          kind:
                            description: kind is the applied resource kind.
                            type: string
                          name:
                            description: name is the applied resource name.
                            type: string
                          uid:
                            description: uid is the last observed UID for safer deletion
                              if the object is recreated.
                            type: string
                        required:
                        - apiVersion
                        - className
                        - kind
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - apiVersion
                      - kind
                      - name
                      x-kubernetes-list-type: map
                  required:
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - namespace
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: NamespaceClassBinding must be named default
          rule: self.metadata.name == 'default'
    served: true
    storage: true
    subresources:
      status: {}
